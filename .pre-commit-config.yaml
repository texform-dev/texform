fail_fast: true

repos:
  # - repo: https://github.com/crate-ci/typos
  #   rev: v1.40.0
  #   hooks:
  #     - id: typos

  - repo: local
    hooks:
      - id: cargo-fmt
        name: cargo fmt
        entry: cargo fmt --
        language: system
        types: [rust]
        pass_filenames: false # This makes it a lot faster

      - id: cargo-clippy
        name: cargo clippy
        language: system
        types: [rust]
        pass_filenames: false
        entry: cargo clippy --all-targets --all-features

      - id: spec-validate-check
        name: spec validate check
        language: system
        files: ^(crates/|resources/specs/)
        pass_filenames: false
        entry: bun run packages/argspecs-validator/src/cli.ts --check

      - id: bench-regression-check
        name: bench regression check
        language: system
        types: [rust]
        pass_filenames: false
        entry: cargo bench -p texform-bench --bench parse_corpus -- --dataset lf80m-benchmarks --check
