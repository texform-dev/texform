characters:
  - name: alpha
    allowed_mode: math
  - name: beta
    allowed_mode: math
  - name: gamma
    allowed_mode: math

commands:
  # Core prefix commands
  - name: frac
    kind: prefix
    allowed_mode: math
    spec: "m m"

  - name: sqrt
    kind: prefix
    allowed_mode: math
    spec: "o m"

  - name: text
    kind: prefix
    allowed_mode: math
    spec: "m:T"

environments:
  - name: matrix
    allowed_mode: math
    spec: ""
    body_mode: math

delimiter_controls: []
